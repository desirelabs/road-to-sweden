import React from "react";
import config from "../lib/config";

export function SocialList({}) {
  return (
    <div>
      <style jsx>{`
        a {
          display: inline-block;
        }
        a:not(:last-child) {
          margin-right: 2em;
        }
      `}</style>
    </div>
  );
}
